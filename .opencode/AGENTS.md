# Project AGENTS.md

This project follows the harness engineering pattern.

See root `AGENTS.md` for the repository map and operating rules.

## Project-Specific Overrides

- Preserve existing uncommitted user changes unless explicitly asked to modify them.
- Prefer `uv run ...` for Python commands.
- Use `bd` for issue tracking when available.
